{
  "name": "hono-newsletter",
  "scripts": {
    "lint": "npx @biomejs/biome lint --write ./src",
    "check": "npx @biomejs/biome check ./src",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify"
  },
  "dependencies": {
    "hono": "^4.6.8"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "@cloudflare/workers-types": "^4.20241022.0",
    "wrangler": "^3.83.0"
  }
}
